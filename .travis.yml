language: python
python:
  - "2.7"

env:
  - TRAVIS_CI=true

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: nosetests

after_success:
  - codecov

services:
  - couchdb
